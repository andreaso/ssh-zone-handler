[Unit]
Description=Primary named server
After=network.target

[Service]
Type=notify
User=primary
ExecStart=/usr/sbin/named -c /etc/primary/named.conf -f
Restart=on-failure
AmbientCapabilities=CAP_NET_BIND_SERVICE
RuntimeDirectory=primary

[Install]
WantedBy=multi-user.target
